# @acme-ai/schemas

`@acme-ai/schemas` 提供 Acme 配置文件和数据结构的 JSON Schema 定义。从 TypeScript
类型生成，支持验证和 IDE 自动补全。

**Schema 类型：**

- Vault 配置 schema
- Project 设置 schema
- Agent 配置 schema
- MCP 服务器配置 schema
- Skill 定义 schema

**工具：** 使用 `schemars` (Rust) 构建，确保健壮的 JSON Schema 生成
